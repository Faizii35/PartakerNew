package com.it.partaker.ItemClickListener

import android.view.View
import com.it.partaker.models.Request

interface MyRequestsClickListener {

    fun OnMyRequestsItemClickListener(view: View, request: Request)
}